<%= form_for(@usage) do |f| %>
  <% if @usage.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@usage.errors.count, "error") %> prohibited this usage from being saved:</h2>

      <ul>
      <% @usage.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :component_id %><br />
    <%= f.number_field :component_id %>
  </div>
  <div class="field">
    <%= f.label :connected_id %><br />
    <%= f.number_field :connected_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
